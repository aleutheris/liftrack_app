# EPIC-260007: Workout days page from content files

> **Plan amendment 2026-09-21, on the owner's instruction, before any code was written.** Once the owner
> supplied the real programme (`program-context.md`), which alternates A and B days across sessions, the
> owner replaced "today by weekday" with paging through every planned day, added reps counted per side and
> a cue line per exercise, and kept the day plans in `content/plan.json` with the Markdown holding the
> rationale. The epic stays `Ready` on that instruction; the bullets and tasks below are the amended plan,
> and [ADR-260008](../adr/ADR-260008.md) carries the matching amendment. The title changed from "Today's
> workout page from content files".

> **Plan amendment 2026-09-23, on the owner's instruction, after the first implementation.** The page
> was measured on REQ-QR-260002's reference connection (1.6 Mbps, 150 ms) against a local build whose
> pictures were ~95 KB incompressible stand-ins for real photos — twice, with matching results: the
> day name and the prescriptions were visible at 0.96 s, the first exercise's pictures decoded at
> 3.9 s (budget 4.0 s), the day settled at 6.9 s, one day transferred 1.2 MB, and 12 of the day's 14
> pictures were fetched without scrolling — native lazy loading deferred 2. That disproves the picture
> budget bullet's premise that only the first exercise's two pictures load on open: at 100 KB a
> picture, a day of real photos misses the 4 s cold open. On the owner's instruction the budget bullet
> below is replaced by **at most 40 KB per picture and at most 600 KB per day**, both checked by the
> content check in CI, and the risks line records what this lab measurement leaves open. The epic
> stays `Ready` and no task changes state; [ADR-260008](../adr/ADR-260008.md)'s Verification records
> the day total, its Decision still deferring the numbers to this epic.

> **Plan amendment 2026-09-24, on the owner's instruction.** A day must show even when its photos do not exist
> yet: an exercise carries up to two pictures, and an empty slot is shown as "Photo N missing" in the same frame.
> The placeholder pictures were removed with the change, so every card names its missing photos until the owner
> adds them; a picture that IS named must still be a file, so a typo still fails CI.
> [ADR-260008](../adr/ADR-260008.md) carries the matching amendment. The epic stays `Ready` and no task changes
> state.

- Epic ID: EPIC-260007
- Title: Workout days page from content files
- Persona/Actor: The owner at the gym, phone in one hand, wanting to see the day's workout without
  signing in.
- Value statement: For the owner at the gym, deliver a page that shows each planned workout day —
  every exercise with two pictures of how to do it, its cue, its sets and its reps — so the programme
  is on the phone from the first release, before sign-in or the backend exist.
- Context: The owner's direction (2026-09-21): start with "something really simple" — a web page fed
  from content stored in files — with sign-in and the Crystord backend to follow. The programme is in
  `program-context.md`: three sessions a week, alternating A and B days, with Days 1–3 written out.
  The six earlier epics are parked (`epic-index.md`). This is the first code in the repository, so it
  is also the first build, the first test suite and the first deploy.
- Problem statement: Nothing is built. Every earlier epic depends on the backend, so none of them
  puts a workout on a phone soon.
- Expected outcome: opening the Pages URL on a phone shows Day 1 — its name and its exercises in
  order, each with two pictures, a cue, the number of sets and the reps per set — and the owner pages
  to any other planned day. Adding a day to the content adds a page; changing the content is a commit,
  and the deploy workflow republishes the site.
- Success metric(s): every day in `plan.json` is reachable on the deployed site; a content commit
  reaches the site with no manual step, and the footer shows its commit; a commit whose content fails
  a check is not deployed; at 360 × 640, sets and reps render at ≥ 24 CSS px, all other text at
  ≥ 16 CSS px, and the paging buttons are at least 48 × 48 CSS px (REQ-QR-260001); the initial
  payload stays ≤ 250 KB compressed, and each picture and each day's pictures stay within the
  budgets below (REQ-QR-260002).
- Non-goals: sign-in; the backend; logging performed sets or weights; deciding which day is today;
  editing the plan in the app (it is edited in the files); showing the exercise pools, slots or
  rules from `program-context.md`; offline support; analytics.
- Constraints: GitHub Pages, static and public (REQ-OR-260001), so the plan and the pictures are
  world-readable; the stack of ADR-260007; the content model of ADR-260008; the page says "sets",
  not "sequences" (ADR-260001); one-thumb use at 360 × 640 (REQ-QR-260001); a slow gym connection —
  1.6 Mbps, 150 ms (REQ-QR-260002).
- Dependencies: the owner creates the GitHub repository and sets Pages → Source to "GitHub Actions"
  before the first deploy; the owner supplies the pictures, each card naming its missing photos until then.
- Risks/assumptions: assumes pictures arrive as full-size phone photos and must be resized to fit
  the budget; a base-path mistake shows up only on the first deploy, as a blank page; with many
  days, paging from Day 1 gets long — revisit if it does. Found in review (2026-09-21): on the
  reference phone, native lazy loading defers few of a day's pictures — measured 2 of 14 on
  2026-09-23, which is why the budgets above assume a day's pictures are nearly all fetched on open.
  That measurement was a local build with stand-ins on the reference profile; what remains is the
  same measurement on the live site, with real photos and the Pages CDN, once the owner supplies
  them. And above 200% browser text size on narrow screens the pager's "Next day" label runs
  off-screen (up to 200% is handled and tested).
- Target window: TBD.
- Validation approach: an end-to-end check on the built site, served under a sub-path — every day
  reachable, a reload keeps the day, and a measurement of text and button sizes at 360 × 640; the
  content check with negative controls in CI; unit and component checks on the pager and the day
  view; and, after each deploy, a check of the live site — the page and every file its HTML
  references load with the right content type, and the served JavaScript carries the deployed
  commit's build id, the one the footer shows. The pictures and each day's page are checked by the
  end-to-end run, on the same build before it is published.

## Ideation Bullets

Resolved in session on 2026-09-21. Bullets marked *Owner* were decided by the owner; the rest were
proposed and approved by the owner at the plan review ("I approve everything"), then amended on the
owner's instruction where the amendment note above says.

- [x] *Owner* — **Stack.** Vite + React + TypeScript, as in `crystord_app` — ADR-260007.
- [x] *Owner* — **Which day is shown.** Every planned day, one per page, in file order; more days in
  the content means more pages. The page does not decide which day is today.
- [x] *Owner* — **Where the data comes from.** Files in the repository, "for now used for content
  only"; the backend replaces them later. Day plans live in `content/plan.json`;
  `program-context.md` keeps the rationale, and its section 10 points to the JSON.
- [x] *Owner* — **What each exercise shows.** Two pictures, the cue line, sets, and reps — with
  "per leg", "per arm" or "per side" where the programme counts each side.
- [x] **Content format and its checks** — ADR-260008: two JSON files that mirror ADR-260001's
  vocabulary, readable slugs as keys, up to two pictures per exercise (amended 2026-09-24), bundled at build
  time and validated in CI.
- [x] **Picture budget: at most 40 KB per picture and at most 600 KB per day**, with fixed
  dimensions and lazy loading after the first exercise; the content check rejects both in CI.
  Amended 2026-09-23 (note above): a day's pictures are nearly all fetched on open, so the budget is
  a whole day's, not the first exercise's. Both numbers come from REQ-QR-260002's 1.6 Mbps
  (≈ 200 KB/s): every day in `plan.json` has seven exercises, so 14 pictures at 40 KB are 0.55 MB,
  about 2.8 s, and 600 KB is about 3 s — leaving the rest of the 4 s cold open for the ≈ 74 KB of
  code the build ships today. A picture is shown in a frame about 143 CSS px wide, so even a 3×
  phone screen shows 429 device px and a 600 px export loses nothing visible; a 3 MB phone photo
  would cost 15 s on its own. Real pictures are resized before they are committed. Three cautions:
  at 14 pictures the per-picture number is what binds a day, the day total catching a day that
  grows past it; ≈ 74 KB is what the build ships today, not what is enforced — the end-to-end check
  allows 250 KB compressed, and at that ceiling the two budgets together would overrun 4 s, so they
  are separate budgets and not a guarantee of the open; and native lazy loading is a small saving,
  not the basis of either number.
- [x] **Base path: relative (`./`)**, so one build works as a project site
  (`<owner>.github.io/<repo>/`) or on a custom domain. The hosting-target question stays in
  EPIC-260002 and must be answered before sign-in, because it decides which sites share Liftrack's
  browser origin (ADR-260005) — it does not block this page.
- [x] **The day in the URL fragment, no router.** `#day-2` keeps the day across a reload or a phone
  restoring the tab; nothing else is routed, and an unknown fragment shows the first day.
- [x] **Build identifier in the footer** (short commit hash and build time). Pages keeps no access
  logs, so this is the only way to tell whether a content commit has reached the phone.
- [x] **Overlap with the parked epics.** This epic builds the scaffold and a reduced Pages pipeline
  (lint, type check, tests, build, deploy) — EPIC-260002's scaffold part, without its backend
  configuration — and a read-only precursor of EPIC-260005's screen. Everything else stays in those
  epics.

## Tasks

Defined at the `Ready` gate (2026-09-21) and amended with the plan. Every task gets a code review.
All tasks were implemented on 2026-09-21 and passed a six-lens adversarial review whose verified
findings were fixed, then committed together as `21c54bc` and pushed directly to `main`, with no
pull request. A second, independent review of that commit (2026-09-21) led to these fixes, made in
the working tree and not yet committed: a check of the live site after each deploy; publishing
rights confined to the deploy job; a picture named twice now fails the content check; the test-side
type check made as strict as the app's and extended to the live-site check, and the CI content check
made independent of the folder it runs from; one unused export removed from the domain types; and
these records and the README brought up to date. Later work, also uncommitted: on 2026-09-22 every
picture was given a square frame showing the whole photo, so an upright photo is no longer cropped;
and on 2026-09-23 the measurement above led to the two picture budgets and their negative controls,
to the live-site check retrying within a deadline instead of a fixed number of attempts — its
parsing and its retry window moved into modules the unit suite covers — and to the Node floor in
`package.json` raised to 22.18, the first 22.x that runs a `.ts` file with `node` unflagged. Each
task still awaits the owner's code review.

| Task | State | Commit/PR/Review |
| --- | --- | --- |
| Scaffold: Vite + React + TypeScript (strict), ESLint, Vitest, `.nvmrc`, npm scripts, relative base path | In Review | `21c54bc` — direct to `main`, no PR; later fixes uncommitted |
| Content: Days 1–3 from `program-context.md` §10 into `content/exercises.json` and `content/plan.json`, pictures named as they are added, and a pointer in §10 | In Review | `21c54bc` — direct to `main`, no PR |
| File-backed source behind the source interface: decode the content into domain terms, pictures resolved by the build | In Review | `21c54bc` — direct to `main`, no PR; later fixes uncommitted |
| Content check in CI, with a negative control for each defect ADR-260008 lists | In Review | `21c54bc` — direct to `main`, no PR; later fixes uncommitted |
| Day pager: every planned day in file order, previous/next and "Day n of N", the day kept in the URL fragment | In Review | `21c54bc` — direct to `main`, no PR |
| Day view: day name; exercises in order, each with two pictures, cue, sets and reps (per side where set); loading and failed states; usable at 360 × 640 | In Review | `21c54bc` — direct to `main`, no PR; later fixes uncommitted |
| Build identifier in the footer | In Review | `21c54bc` — direct to `main`, no PR |
| GitHub Actions workflow: lint, type check, tests, build, end-to-end check, then publish to Pages | In Review | `21c54bc` — direct to `main`, no PR; later fixes uncommitted |
| End-to-end check: built site served under a sub-path — every day reachable, reload keeps the day, text and button sizes at 360 × 640, no failed requests | In Review | `21c54bc` — direct to `main`, no PR; later fixes uncommitted |
| README: run locally, edit the content, add pictures, deploy | In Review | `21c54bc` — direct to `main`, no PR; later fixes uncommitted |
| Full code + test coverage for this epic | In Review | `21c54bc` — direct to `main`, no PR; later fixes uncommitted |

## Links (durable records spawned)

- Requirements: [REQ-FR-260003](../requirements/REQ-FR-260003.md) (partly: the day's exercises in
  planned order with sets and reps — but the owner pages to the day rather than the page selecting it,
  and there is no logging), [REQ-OR-260001](../requirements/REQ-OR-260001.md) (static files and every
  asset under the base path; its endpoint and profile criteria stay with EPIC-260002),
  [REQ-QR-260001](../requirements/REQ-QR-260001.md), [REQ-QR-260002](../requirements/REQ-QR-260002.md)
- ADRs: [ADR-260007](../adr/ADR-260007.md) (stack), [ADR-260008](../adr/ADR-260008.md) (content
  from files), [ADR-260001](../adr/ADR-260001.md) (the vocabulary the content mirrors)
- ICRs: n/a — no interface changes
- Contracts: n/a — the content format is internal and recorded in ADR-260008;
  [liftrack-atom-shape-contract.md](../contracts/liftrack-atom-shape-contract.md) is its eventual
  backend form
