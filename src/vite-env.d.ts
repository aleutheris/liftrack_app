/// <reference types="vite/client" />

/** Short commit hash of the build, or "local" outside CI (see vite.config.ts). */
declare const __BUILD_COMMIT__: string
/** ISO-8601 time the build ran (see vite.config.ts). */
declare const __BUILD_TIME__: string
